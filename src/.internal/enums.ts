export enum EClasses {
  ROOT = 'root',
  TOOLBAR = 'toolbar',
  BUTTON = 'button',
  SELECT = 'select',
  CONTENT = 'content',
}

export enum ENodeType {
  ELEMENT = 1,
  ATTRIBUTE,
  TEXT,
  CDATA_SECTION,
  ENTITY_REFERENCE,
  ENTITY,
  PROCESSING_INSTRUCION,
  COMMENT,
  DOCUMENT,
  DOCUMENT_TYPE,
  DOCUMENT_FRAGMENT,
  NOTATION,
}
